//Init pagination controller example

//Todo.....
/*
late CustomPaginationController<{ModelName}> paginationController =
CustomPaginationController<{ModelName}>(
    onFetchPage: (pageKey, filterText, searchText) async => await apiCallFunction(pageKey, filterText, searchText));*/

//Then initialize controller inside view...
//Todo:

/*
* PagingView<{ModelName}>(
          pagingController: controller.paginationController.pagingController,
          itemBuilder: (context, item, i) => Widget(model: item),
        )
* */

//Refresh Pagination...
//Todo:

/*
paginationController.refresh(filter: value, search:value);*/
